{% extends 'dashboard/index.html.twig' %}

{% block title %}Register{% endblock %}

{% block dashboardBody %}

    <h1>Edit Warehouse</h1>

    {{ include('warehouse/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('app_warehouse_index') }}">back to list</a>

    {{ include('warehouse/_delete_form.html.twig') }}

{% endblock %}
